"""BoneIO extension modules.

Each subpackage is a self-contained feature that lives alongside core boneIO
without modifying its internals. Modules expose a stable public API via their
``__init__.py``; core boneIO files import only from there.
"""
