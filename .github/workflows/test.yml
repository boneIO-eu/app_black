name: Tests

on:
  push:
    branches: [dev-debian13]
  pull_request:
    branches: [dev-debian13]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.13"]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install test dependencies
        run: |
          pip install pytest pytest-asyncio pytest-cov
          pip install -e . --no-deps || true
          pip install aiomqtt pydantic PyYAML colorlog Cerberus psutil aiohttp smbus2 pymodbus

      - name: Run unit tests
        run: pytest tests/unit -v --tb=short -m "not hardware"

      - name: Run tests with coverage
        run: pytest tests/unit -v --cov=boneio --cov-report=xml --cov-report=term -m "not hardware"

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v4
        with:
          file: ./coverage.xml
          fail_ci_if_error: false
        continue-on-error: true

  # TODO: Enable lint job after running: black boneio tests && isort boneio tests
  # lint:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #
  #     - name: Set up Python
  #       uses: actions/setup-python@v5
  #       with:
  #         python-version: "3.13"
  #
  #     - name: Install lint dependencies
  #       run: pip install black isort flake8
  #
  #     - name: Run linters
  #       run: |
  #         black --check boneio tests
  #         isort --check-only boneio tests
  #         flake8 boneio --count --select=E9,F63,F7,F82 --show-source --statistics
