/**
 * Stands in for the `prettier/plugins/*` bundles monaco-yaml hands to Prettier.
 * Only ever referenced by the stubbed `format`, which throws before using them.
 * See src/stubs/prettier-standalone.ts.
 */
export default {};
